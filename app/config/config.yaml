# ==========================================
# Configuration pour le projet qwertz
# ==========================================
# 
# Les variables SSL/TLS sont désormais gérées
# via les variables d'environnement (.env)
# 
# Voir .env.example pour les détails

# Cette section est conservée pour compatibilité
bitbucket:
  url: https://bitbucket.company.net

bigip:
  # Configuration BIG-IP
  # Les credentials et SSL sont gérés via .env
